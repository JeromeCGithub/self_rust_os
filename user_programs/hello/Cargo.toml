[package]
name = "hello"
version = "0.1.0"
edition = "2021"
description = "A minimal user-space program for self_rust_os."
license = "MIT OR Apache-2.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
